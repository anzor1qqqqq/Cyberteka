import * as React from 'react';

import { IChildrenElement } from "@/types/types";

export default function LayoutProfClub ({children}: IChildrenElement) {
    return (
        <>
            {children}  
        </>
    );
};